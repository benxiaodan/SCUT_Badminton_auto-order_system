# 本地开发环境配置
# 端口与其他配置
SCUT_PORT=5002
SCUT_HOST=127.0.0.1
SCUT_ADMIN_ONLY=0

# 白名单文件（本地测试用）
SCUT_ALLOWLIST_FILE=test_allowed_users.txt

# 添加你的 Gemini API Key (保留原有的)

# 邮件服务配置 (本地测试可留空或使用相同配置)
SMTP_SERVER=smtp.qq.com
SMTP_PORT=465
SMTP_SENDER=1696725502@qq.com
SMTP_PASSWORD=voqujocowzfrccdh

